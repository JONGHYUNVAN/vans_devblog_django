# =============================================================================
# VansDevBlog Search Service - CloudType.io Requirements
# CloudType.io 배포용 패키지 목록
# =============================================================================

# 기본 Django 패키지
Django==4.2.7
djangorestframework==3.14.0
django-cors-headers==4.3.1

# 정적 파일 처리
whitenoise==6.6.0

# 데이터베이스
dj-database-url==2.1.0
psycopg2-binary==2.9.7

# 캐싱 (선택사항)
django-redis==5.4.0
redis==5.0.1

# Elasticsearch
django-elasticsearch-dsl==7.3.0
elasticsearch==7.17.9

# MongoDB
pymongo==4.5.0

# MariaDB/MySQL (검색 로그용)
PyMySQL==1.1.0

# API 문서화
drf-yasg==1.21.7

# 환경 변수
python-dotenv==1.0.0

# 데이터 검증
pydantic==2.1.1

# WSGI 서버
gunicorn==21.2.0

# 유틸리티
requests==2.31.0
python-dateutil==2.8.2

# 에러 모니터링 (선택사항)
# sentry-sdk==1.38.0

# AWS S3 정적 파일 (선택사항)
# django-storages==1.14.0
# boto3==1.29.0

# 환경 최적화
uvloop==0.19.0  # 비동기 처리 성능 향상 (Unix 계열만)
setproctitle==1.3.3  # 프로세스 이름 설정


